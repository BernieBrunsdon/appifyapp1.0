# Production build defaults. Note: if app/.env.local sets REACT_APP_APP_SHELL=1, it can still
# end up in the bundle — App.js always shows marketing on appifyai.com anyway.
# Optional: create app/.env.production.local (gitignored) with REACT_APP_APP_SHELL= for clean CI builds.
